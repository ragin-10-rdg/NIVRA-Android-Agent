<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network telemetry delivery to the Wazuh Manager -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Keep the collection pipeline alive as a foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- NOTE: NIVRA intentionally does NOT request READ_SMS, READ_CONTACTS, camera/mic,
         location, or storage permissions. Per the Legal/Ethical scope, it only reads
         device state, security configuration, app inventory metadata, SecurityLog
         events, and DPC-mediated network metadata; it never reads message/file content. -->

    <application
        android:name=".NivraApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Nivra">

        <!-- Explicit App Drawer presence: normal launchable application,
             not a hidden/background-only component. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Nivra"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Device Policy Controller receiver. Must be provisioned as Device Owner
             via `adb shell dpm set-device-owner com.nivra.agent/.dpc.NivraDeviceAdminReceiver`
             on a factory-reset / unprovisioned test device (see README), or via the
             QR/zero-touch provisioning payload in /provisioning for fleet deployment. -->
        <receiver
            android:name=".dpc.NivraDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.PROFILE_PROVISIONING_COMPLETE" />
                <action android:name="android.app.action.DEVICE_OWNER_CHANGED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".agent.AgentService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver android:name=".agent.BootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
